# Copy to .env and fill in values

VITE_API_URL=/api

# From https://dashboard.vapi.ai (must match backend VAPI_* assistant + public key)
VITE_VAPI_PUBLIC_KEY=
VITE_VAPI_ASSISTANT_ID=
VITE_ELEVENLABS_VOICE_ID=
# When both are set, browser mic/TTS is disabled — voice uses Vapi + ElevenLabs only
